<form autocomplete="off">
  {{> "genefunk2090.advancement-controls"}}
  <div class="form-group">
    <label>{{localize "GENEFUNK2090.AdvancementItemGrantOptional"}}</label>
    <div class="form-fields">
      <input type="checkbox" name="configuration.optional" {{checked configuration.optional}}>
    </div>
    <p class="hint">{{localize "GENEFUNK2090.AdvancementItemGrantOptionalHint"}}</p>
  </div>

  {{#if showSpellConfig}}
    {{> "genefunk2090.advancement-spell-config"}}
  {{/if}}

  <div class="drop-target">
    <ol class="items-list">
      <li class="items-header flexrow"><h3 class="item-name">{{localize "DOCUMENT.Items"}}</h3></li>
      <ol class="item-list">
      {{#each configuration.items}}
        <li class="item flexrow" data-item-uuid="{{this}}">
          <div class="item-name">{{{genefunk2090-linkForUuid this}}}</div>
          <div class="item-controls flexrow">
            <a class="item-control item-action" data-action="delete" data-tooltip="GENEFUNK2090.ItemDelete">
              <i class="fas fa-trash"></i>
            </a>
          </div>
        </li>
      {{/each}}
      </ol>
    </ol>

    <p class="hint centered">{{localize "GENEFUNK2090.AdvancementItemGrantDropHint"}}</p>
  </div>
</form>
