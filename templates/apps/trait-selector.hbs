<form autocomplete="off" onsubmit="event.preventDefault();">
    {{> "genefunk2090.trait-list" prefix="choices"}}

    {{#if customPath}}
        <div class="form-group stacked">
            <label>{{localize "GENEFUNK2090.TraitSelectorSpecial"}}</label>
            <div class="form-fields">
                <input type="text" name="{{customPath}}" value="{{custom}}">
            </div>
        </div>
    {{/if}}

    {{#if bypassesPath}}
        <h3>{{localize "GENEFUNK2090.DamagePhysicalBypass"}}</h3>
        <p class="note">{{localize "GENEFUNK2090.DamagePhysicalBypassHint"}}</p>
        {{> "genefunk2090.trait-list" choices=bypasses prefix="bypasses"}}
    {{/if}}

    <button type="submit"><i class="far fa-save"></i> {{localize "GENEFUNK2090.TraitSave"}}</button>
</form>
